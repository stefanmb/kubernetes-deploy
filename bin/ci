#!/bin/bash
set -euo pipefail

if [[ -n "${DEBUG:+set}" ]]; then
  set -x
fi

docker run --rm \
	--net=host \
	-v "$HOME/.kube":/"$HOME/.kube" \
    -v "$PWD":/usr/src/app \
    -w /usr/src/app \
    ruby:2.3 \
    sh -c "bundle install --jobs 4 && bundle exec rake test"